# PC2 ROS 2 workspace v2.0.0

Branch:

```text
IONIQ_EV/PC2/ros2_ws/v2.0.0
```

This parentless source snapshot contains the Lucid camera driver used by the PC2 dual-camera
pipeline. Build, install, log, cache, runtime-state, and nested Git files are intentionally omitted.

See `src/lucid_vision_driver/PC2_V2.0.0.md` for camera identities, launch architecture, reliability
changes, validation evidence, calibration limits, and the external Arena SDK prerequisite.
