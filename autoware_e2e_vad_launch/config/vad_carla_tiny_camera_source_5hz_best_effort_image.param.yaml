/**:
  ros__parameters:
    sync_params:
      # Match the six best-effort raw-image writers. The synchronized frame
      # buffer remains bounded and camera_info transport remains reliable.
      frame_buffer_size: 32
      image_queue_depth: 32
      image_reliability: best_effort
