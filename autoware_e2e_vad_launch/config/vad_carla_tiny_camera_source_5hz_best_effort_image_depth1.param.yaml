/**:
  ros__parameters:
    sync_params:
      # Match the six bounded-latest Best-Effort raw-image writers. The
      # synchronized frame buffer remains bounded independently of DDS history.
      frame_buffer_size: 32
      image_queue_depth: 1
      image_reliability: best_effort
