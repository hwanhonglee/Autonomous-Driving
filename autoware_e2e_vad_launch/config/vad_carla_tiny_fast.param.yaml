/**:
  ros__parameters:
    node_params:
      # Raw 640x360 avoids six JPEG encode/decode paths on the same host.
      use_raw: [true, true, true, true, true, true]

    interface_params:
      input_image_width: 640
      input_image_height: 360
