/**:
  ros__parameters:
    sync_params:
      # Selected after CARLA closed-loop A/B: bounded buffering plus matched
      # reliable camera transport improves accepted VAD updates without changing precision.
      frame_buffer_size: 32
      image_queue_depth: 32
      image_reliability: reliable
