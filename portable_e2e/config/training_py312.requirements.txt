# Non-PyTorch runtime dependencies for the isolated Pro6000 Python 3.12 venv.
# PyTorch uses its own CUDA-specific index and is installed in a separate,
# explicitly reviewed command; see docs/portable-e2e-training.md.
numpy==2.5.2
Pillow==12.3.0
