<?xml version="1.0" encoding="UTF-8"?>
<launch>
  <!-- Essential parameters -->
  <arg name="map_path" description="point cloud and lanelet2 map directory path"/>
  <arg name="vehicle_model" default="sample_vehicle" description="vehicle model name"/>
  <arg name="sensor_model" default="sample_sensor_kit" description="sensor model name"/>
  <arg name="pointcloud_container_name" default="pointcloud_container"/>
  <!-- HH_260811 - Give PC3's otherwise empty generic container a host-unique identity on the shared DDS graph. -->
  <arg name="generic_pointcloud_container_name" default="pc3_pointcloud_container"/>
  <!-- HH_260811 - Restored the original default that launches the generic pointcloud container. -->
  <arg name="launch_pointcloud_container" default="true" description="launch the generic pointcloud container"/>
  <arg name="data_path" default="$(env HOME)/autoware_data" description="packages data and artifacts directory path"/>

  <!-- launch module preset -->
  <arg name="planning_module_preset" default="default" description="planning module preset"/>

  <!-- Optional parameters -->
  <!-- Modules to be launched -->
  <arg name="launch_vehicle" default="true" description="launch vehicle"/>
  <arg name="launch_system" default="true" description="launch system"/>
  <arg name="launch_map" default="true" description="launch map"/>
  <arg name="launch_sensing" default="true" description="launch sensing"/>
  <!-- HH_260811 - Restored the original full PC3 bring-up defaults for normal run_autoware use. -->
  <arg name="launch_sensing_driver" default="true" description="launch sensing driver"/>
  <arg name="launch_localization" default="true" description="launch localization"/>
  <arg name="launch_perception" default="true" description="launch perception"/>
  <arg name="launch_planning" default="true" description="launch planning"/>
  <arg name="launch_control" default="true" description="launch control"/>
  <arg name="launch_api" default="true" description="launch api"/>
  <!-- Global parameters -->
  <arg name="use_sim_time" default="false" description="use_sim_time"/>
  <!-- Vehicle -->
  <arg name="vehicle_id" default="$(env VEHICLE_ID default)" description="vehicle specific ID"/>
  <!-- HH_260811 - Restored the original vehicle-interface default for the normal full launch. -->
  <arg name="launch_vehicle_interface" default="true" description="launch vehicle interface"/>
  <!-- Control -->
  <arg name="check_external_emergency_heartbeat" default="false"/>
  <!-- Map -->
  <arg name="lanelet2_map_file" default="lanelet2_map.osm" description="lanelet2 map file name"/>
  <arg name="pointcloud_map_file" default="pointcloud_map.pcd" description="pointcloud map file name"/>
  <!-- System -->
  <arg name="system_run_mode" default="online" description="run mode in system"/>
  <arg name="launch_system_monitor" default="true" description="launch system monitor"/>
  <arg name="launch_dummy_diag_publisher" default="false" description="launch dummy diag publisher"/>
  <!-- HH_260811 - Restored the system module's original MRM-enabled default. -->
  <arg name="launch_mrm" default="true" description="launch MRM command publishers"/>
  <arg name="diagnostic_graph_aggregator_graph_path" default="$(find-pkg-share autoware_launch)/config/system/diagnostic_graph_aggregator/autoware-main.yaml" description="diagnostic graph config"/>
  <!-- HH_260811 - Use a PC3 safety graph that does not require a local MRM command publisher when PC1 owns actuation. -->
  <arg name="diagnostic_graph_aggregator_graph_path_no_mrm" default="$(find-pkg-share autoware_launch)/config/system/diagnostic_graph_aggregator/autoware-main-pc3-no-mrm.yaml" description="diagnostic graph config used when MRM publishers are disabled"/>
  <let name="active_diagnostic_graph_aggregator_graph_path" value="$(var diagnostic_graph_aggregator_graph_path)" if="$(var launch_mrm)"/>
  <let name="active_diagnostic_graph_aggregator_graph_path" value="$(var diagnostic_graph_aggregator_graph_path_no_mrm)" unless="$(var launch_mrm)"/>
  <!-- Tools -->
  <!-- HH_260811 - Restored the original RViz startup default for visible bring-up status. -->
  <arg name="rviz" default="true" description="launch rviz"/>
  <arg name="rviz_config" default="$(find-pkg-share autoware_launch)/rviz/autoware.rviz" description="rviz config"/>
  <!-- HH_260811 - Keep RViz enabled on every PC while making the PC3 node name unique. -->
  <arg name="rviz_node_name" default="rviz2_pc3" description="host-unique RViz node name"/>
  <!-- HH_260811 - Restored the original RViz respawn behavior. -->
  <arg name="rviz_respawn" default="true"/>
  <!-- Perception -->
  <arg name="perception_mode" default="lidar" description="select perception mode. camera_lidar_radar_fusion, camera_lidar_fusion, lidar_radar_fusion, lidar, radar"/>
  <arg name="traffic_light_recognition/enable_fine_detection" default="true" description="enable traffic light fine detection"/>
  <!-- Auto mode setting-->
  <arg name="enable_all_modules_auto_mode" default="false" description="enable all module's auto mode"/>
  <arg name="is_simulation" default="false" description="Autoware's behavior will change depending on whether this is a simulation or not."/>

  <!-- Global parameters -->
  <group scoped="false">
    <include file="$(find-pkg-share global_parameter_loader)/launch/global_params.launch.py">
      <arg name="use_sim_time" value="$(var use_sim_time)"/>
      <arg name="vehicle_model" value="$(var vehicle_model)"/>
    </include>
  </group>

  <!-- Pointcloud container -->
  <!-- HH_260811 - Keep the restored generic container but isolate its name from PC2 and the sensing-owned container. -->
  <group if="$(var launch_pointcloud_container)">
    <include file="$(find-pkg-share autoware_launch)/launch/pointcloud_container.launch.py">
      <arg name="use_multithread" value="true"/>
      <arg name="container_name" value="$(var generic_pointcloud_container_name)"/>
    </include>
  </group>

  <!-- Vehicle -->
  <group if="$(var launch_vehicle)">
    <include file="$(find-pkg-share tier4_vehicle_launch)/launch/vehicle.launch.xml">
      <arg name="vehicle_model" value="$(var vehicle_model)"/>
      <arg name="sensor_model" value="$(var sensor_model)"/>
      <arg name="vehicle_id" value="$(var vehicle_id)"/>
      <arg name="launch_vehicle_interface" value="$(var launch_vehicle_interface)"/>
      <arg name="config_dir" value="$(find-pkg-share individual_params)/config/$(var vehicle_id)/$(var sensor_model)"/>
      <arg name="raw_vehicle_cmd_converter_param_path" value="$(find-pkg-share autoware_launch)/config/vehicle/raw_vehicle_cmd_converter/raw_vehicle_cmd_converter.param.yaml"/>
    </include>
  </group>

  <!-- System -->
  <group if="$(var launch_system)">
    <!-- HH_260810 - Forwarded the stationary-test MRM gate while retaining non-command system services. -->
    <include file="$(find-pkg-share autoware_launch)/launch/components/tier4_system_component.launch.xml">
      <arg name="launch_mrm" value="$(var launch_mrm)"/>
      <!-- HH_260811 - Select the matching diagnostic contract together with the MRM ownership gate. -->
      <arg name="diagnostic_graph_aggregator_graph_path" value="$(var active_diagnostic_graph_aggregator_graph_path)"/>
    </include>
  </group>

  <!-- Map -->
  <group if="$(var launch_map)">
    <include file="$(find-pkg-share autoware_launch)/launch/components/tier4_map_component.launch.xml"/>
  </group>

  <!-- Sensing -->
  <group if="$(var launch_sensing)">
    <include file="$(find-pkg-share autoware_launch)/launch/components/tier4_sensing_component.launch.xml"/>
  </group>

  <!-- Localization -->
  <group if="$(var launch_localization)">
    <!-- HH_260810 - Attached localization preprocessing to PC3's sensor-owned pointcloud container. -->
    <include file="$(find-pkg-share autoware_launch)/launch/components/tier4_localization_component.launch.xml">
      <arg name="localization_pointcloud_container_name" value="/sensing/lidar/top/pointcloud_preprocessor/$(var pointcloud_container_name)"/>
    </include>
  </group>

  <!-- Tools -->
  <group>
    <node
      pkg="rviz2"
      exec="rviz2"
      name="$(var rviz_node_name)"
      output="screen"
      args="-d $(var rviz_config) -s $(find-pkg-share autoware_launch)/rviz/image/autoware.png"
      if="$(var rviz)"
      respawn="$(var rviz_respawn)"
    >
      <env name="QT_QPA_PLATFORMTHEME" value="qt5ct"/>
      <!-- HH_260811 - Prevent Snap-hosted IDE terminals from injecting an incompatible GTK library path into RViz. -->
      <env name="GTK_PATH" value=""/>
    </node>
  </group>
</launch>
