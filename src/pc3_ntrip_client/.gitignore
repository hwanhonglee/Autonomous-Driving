# HH_260811 - Keep runtime credentials outside this repository and ignore accidental local variants.
# The canonical config/ntrip_config.yaml is a tracked placeholder template and must never contain secrets.
/config/*.local.yaml
/config/*.private.yaml

__pycache__/
.pytest_cache/
